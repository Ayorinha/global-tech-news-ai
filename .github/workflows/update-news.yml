name: Atualizar Noticias

on:
  schedule:
    - cron: "0 * * * *"
  push:
    branches:
      - main
      - master
  workflow_dispatch:

# Evita que duas execucoes do mesmo workflow tentem publicar
# simultaneamente no branch principal.
concurrency:
  group: atualizar-noticias-main
  cancel-in-progress: false

jobs:
  fetch-and-deploy:
    name: Coletar Noticias e Publicar
    runs-on: ubuntu-latest

    permissions:
      contents: write

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          fetch-depth: 0

      - name: Configurar Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Instalar dependencias
        run: pip install feedparser requests deep-translator

      - name: Sincronizar com main antes da coleta
        run: |
          git fetch origin main
          git rebase origin/main

      - name: Coletar e traduzir noticias
        run: python scripts/fetch_news.py

      - name: Commitar e publicar news.json
        run: |
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          git config --global user.name "github-actions[bot]"
          git add data/news.json
          if git diff --staged --quiet; then
            echo "Nenhuma mudanca para commitar."
          else
            git commit -m "chore: atualizar noticias $(date -u +'%Y-%m-%d %H:%M UTC')"

            # Atualiza novamente antes do push para evitar non-fast-forward
            # caso o main tenha recebido uma alteracao durante a coleta.
            git fetch origin main
            git rebase origin/main
            git push origin HEAD:main
          fi
