# Endpoint Agent Research

The future native agent is the protected background service/daemon. Initial implementation must be read-only and development-only.

Responsibilities: telemetry, local policy evaluation, health, secure IPC, signed updates and evidence buffering.

Enforcement comes only after platform-specific security review.