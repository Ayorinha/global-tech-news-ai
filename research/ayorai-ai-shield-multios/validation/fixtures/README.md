# Validation Fixtures

This directory is reserved for deterministic, non-malicious research fixtures.

Fixtures must not contain:

- real credentials;
- malware;
- exploit payloads;
- personal data;
- unauthorized targets;
- destructive commands.

Use synthetic strings and metadata only.
